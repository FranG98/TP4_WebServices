export class Cotiza {
    //to-type: string;
    //from-value: number;
    //from-type: string;
    result: string;
    convert: number;

    cotiza(/*to-type?: string, from-value?: number, from-type?: string, */result?: string, convert?: number){
      /*  this.id = id;
        this.seo = seo;
        this.tit = tit;*/
        this.result = result;
        this.convert = convert;
    }
} 
